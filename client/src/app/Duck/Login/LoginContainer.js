import React, { Compoent } from 'react';
import styled from 'styled-components';

export default styled.div`
    height: 8   35px;
    overflow: hidden;
    /* position: relative; */
    /* z-index: -1; */
    background: #DCE1E1;
`