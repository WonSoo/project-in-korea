import React, { PureComponent } from 'react';
import styled from 'styled-components';

const LeftButton = styled.span`
    padding: 17px 10px;
    height: 30px;
    font-weight: 900;
`;

export default () => (
    <LeftButton>로그인</LeftButton>
)